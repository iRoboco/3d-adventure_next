diff --git a/FirstPersonController.h b/FirstPersonController.h
index 1234567..abcdefg 100644
--- a/FirstPersonController.h
+++ b/FirstPersonController.h
@@ -114,6 +114,12 @@ public:
  */
 void setRaycaster(VoxelRaycasterNode* raycaster) { _raycaster = raycaster; }
 
+// ========================================================================
+/** @name Взаимодействие с блоками (перенесено из GameScene)
+*  @{ */
+// ========================================================================
+void processBlockInteraction(float dt);
+/// @}
 
 /// @} // конец группы "Интеграция с миром "
 
@@ -285,6 +291,14 @@ private:
 // ========================================================================
 /** @name Интеграция с миром
  *  @{ */
+// ========================================================================
+
+VoxelRaycasterNode* _raycaster = nullptr; ///< Ссылка на рейкастер (не владеет)
+BlockId             _selectedBlockId = BLOCK_STONE; ///< ID блока для установки
+float               _breakAccum = 0.0f;             ///< Накопленное время удержания ЛКМ
+static constexpr float BREAK_TIME = 0.8f;           ///< Секунд до разрушения блока
+
+// ========================================================================
+/// @}
+
 ChunkManager* _chunkMgr = nullptr; ///< Ссылка на менеджер чанков для запросов блоков
 ///< Используется только для чтения: getBlockAtWorldPos(), isChunkActive()
 
diff --git a/FirstPersonController.cpp b/FirstPersonController.cpp
index 1234567..abcdefg 100644
--- a/FirstPersonController.cpp
+++ b/FirstPersonController.cpp
@@ -38,7 +38,8 @@ bool FirstPersonController::init(ax::Camera* camera, float moveSpeed, float mous
 	setupEventListeners();
 	setFreeFlightMode(false);
-	scheduleUpdate();
+	// Приоритет 0: выполняется ПЕРВЫМ, чтобы позиция камеры обновилась до каста луча в рейкастере
+	scheduleUpdateWithPriority(0);
 	return true;
 }
 
@@ -152,13 +153,41 @@ void FirstPersonController::setupEventListeners()
 _mouseListener = ax::EventListenerMouse::create();
 _mouseListener->onMouseMove = [this](auto* event) { this->onMouseMove(event); return true; };
-_mouseListener->onMouseDown = [this](auto* event) { this->onMouseDown(event); return true; };
-_mouseListener->onMouseUp = [this](auto* event) { this->onMouseUp(event); return true; };
+_mouseListener->onMouseDown = [this](auto* event) {
+    auto* e = static_cast<ax::EventMouse*>(event);
+    // ЛКМ в freeFlight — только для вращения камеры
+    if (e->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_LEFT && _freeFlightMode) {
+        _isLeftMousePressed = true;
+        _lastMousePos.set(e->getCursorX(), e->getCursorY());
+    }
+    // В режиме FPS LMB/RMB управляются через VoxelRaycaster
+    if (!_freeFlightMode && _raycaster) {
+        if (e->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_LEFT) {
+            _raycaster->setInteractionMode(BlockInteractionMode::BreakHold);
+            _breakAccum = 0.0f;
+            _raycaster->setBreakProgress(0.0f);
+        } else if (e->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_RIGHT) {
+            _raycaster->setInteractionMode(BlockInteractionMode::PlaceIntent);
+        }
+    }
+    return true;
+};
+_mouseListener->onMouseUp = [this](auto* event) {
+    auto* e = static_cast<ax::EventMouse*>(event);
+    if (e->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_LEFT) {
+        _isLeftMousePressed = false;
+        if (!_freeFlightMode && _raycaster) {
+            _raycaster->setInteractionMode(BlockInteractionMode::Look);
+            _breakAccum = 0.0f;
+            _raycaster->setBreakProgress(0.0f);
+        }
+    } else if (e->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_RIGHT) {
+        if (!_freeFlightMode && _raycaster) {
+            _raycaster->placeBlock(_selectedBlockId);
+            _raycaster->setInteractionMode(BlockInteractionMode::Look);
+        }
+    }
+    return true;
+};
 // 🔧 Mouse listener привязан к scene graph — снимается автоматически при удалении ноды
 _eventDispatcher->addEventListenerWithSceneGraphPriority(_mouseListener, this);
 
@@ -235,6 +264,12 @@ void FirstPersonController::onKeyPressed(ax::EventKeyboard::KeyCode code, ax::Ev
 case ax::EventKeyboard::KeyCode::KEY_SPACE: _keySpace = true; break;
 case ax::EventKeyboard::KeyCode::KEY_F5: toggleFlightMode(); break;
+// Цифры 1-3 — выбор блока для установки
+case ax::EventKeyboard::KeyCode::KEY_1: _selectedBlockId = BLOCK_STONE; break;
+case ax::EventKeyboard::KeyCode::KEY_2: _selectedBlockId = BLOCK_DIRT;  break;
+case ax::EventKeyboard::KeyCode::KEY_3: _selectedBlockId = BLOCK_GRASS; break;
 default: break;
 }
 }
 
@@ -258,8 +293,11 @@ void FirstPersonController::update(float dt)
     this->setPosition3D(result.resolvedPosition);
 }
 
-// Обработка таймингов разрушения и переключения режимов (вызывается после физики)
-processBlockInteraction(dt);
+    // Обработка таймингов разрушения и переключения режимов (вызывается после физики)
+    processBlockInteraction(dt);
+}
+
+// =========================================================================
+//  processBlockInteraction — накопление прогресса разрушения
+// =========================================================================
+void FirstPersonController::processBlockInteraction(float dt)
+{
+    // Быстрый выход если рейкастер не инициализирован или не в режиме разрушения
+    if (!_raycaster || _raycaster->getInteractionMode() != BlockInteractionMode::BreakHold) {
+        return;
+    }
+
+    // Если луч никуда не попал — прогресс не копится
+    if (!_raycaster->hasHit()) {
+        _breakAccum = 0.0f;
+        _raycaster->setBreakProgress(0.0f);
+        return;
+    }
+
+    _breakAccum += dt;
+    float progress = std::min(_breakAccum / BREAK_TIME, 1.0f);
+    _raycaster->setBreakProgress(progress);
+
+    // Прогресс достиг 100% → разрушаем блок
+    if (progress >= 1.0f) {
+        _raycaster->breakBlock();
+        _breakAccum = 0.0f;
+        _raycaster->setBreakProgress(0.0f);
+    }
 }
 
 // =========================================================================
@@ -334,3 +372,6 @@ void FirstPersonController::onMouseUp(ax::Event* event)
 if (e->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_LEFT) {
 _isLeftMousePressed = false;
 }
+}
+
+// Конец файла FirstPersonController.cpp
diff --git a/GameScene.h b/GameScene.h
index 1234567..abcdefg 100644
--- a/GameScene.h
+++ b/GameScene.h
@@ -18,14 +18,8 @@ protected:
 void onEnter() override;
 void onExit() override;
 private:
-void update(float dt) override;
-void onKeyPressed(ax::EventKeyboard::KeyCode keyCode, ax::Event* event);
-// === Управление взаимодействием ===
-void setupInteractionListeners();  // Мышь: режимы + тайминги
-void processBreakProgress(float dt); // Накопление прогресса разрушения
+void update(float dt) override;
+void onKeyPressed(ax::EventKeyboard::KeyCode keyCode, ax::Event* event);
 
 // === Данные ===
 ChunkManager            _chunkMgr;
@@ -33,11 +27,6 @@ private:
 ax::Camera*             _mainCamera       = nullptr;
 ax::Node*               _playerNode       = nullptr;
 VoxelRaycasterNode*     _raycaster        = nullptr;
-
-// === Состояние разрушения ===
-float _breakAccum = 0.0f;
-static constexpr float BREAK_TIME = 0.8f;  // секунды до разрушения блока
-
-// === Выбранный блок для установки ===
-BlockId _selectedBlockId = BLOCK_STONE;
 };
diff --git a/GameScene.cpp b/GameScene.cpp
index 1234567..abcdefg 100644
--- a/GameScene.cpp
+++ b/GameScene.cpp
@@ -94,18 +94,10 @@ bool GameScene::init()
 _raycaster->setBreakProgressColor({1.0f, 0.3f, 0.1f, 0.5f});
 
 // Коллбек при наведении — меняем цвет highlight по типу блока
 _raycaster->setOnHit([this](const VoxelHit& hit) {
     switch (hit.blockId) {
     case BLOCK_GRASS: _raycaster->setFaceHighlightColor({0.2f, 0.8f, 0.2f, 0.3f}); break;
     case BLOCK_STONE: _raycaster->setFaceHighlightColor({0.6f, 0.6f, 0.6f, 0.3f}); break;
     case BLOCK_DIRT:  _raycaster->setFaceHighlightColor({0.5f, 0.35f, 0.2f, 0.3f}); break;
     default:          _raycaster->setFaceHighlightColor({1.0f, 1.0f, 1.0f, 0.25f}); break;
     }
 });
-_raycaster->setOnMiss([this]() {
-    // Сбрасываем режим при промахе
-    _raycaster->setInteractionMode(BlockInteractionMode::Look);
-    _breakAccum = 0.0f;
-});
 this->addChild(_raycaster);
 
 // Передаём рейкастер в контроллер
@@ -115,9 +107,6 @@ bool GameScene::init()
 auto kbListener = ax::EventListenerKeyboard::create();
 kbListener->onKeyPressed = AX_CALLBACK_2(GameScene::onKeyPressed, this);
 _eventDispatcher->addEventListenerWithSceneGraphPriority(kbListener, this);
-setupInteractionListeners();
 scheduleUpdate();
 _chunkMgr.forceUpdate();
 return true;
@@ -125,54 +114,6 @@ return true;
 // ============================================================================
 
-// ============================================================================
-//  setupInteractionListeners — intent-driven управление мышью
-// ============================================================================
-void GameScene::setupInteractionListeners()
-{
-    auto ml = ax::EventListenerMouse::create();
-    ml->onMouseDown = [this](ax::Event* event) {
-        auto* me = static_cast<ax::EventMouse*>(event);
-        if (me->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_RIGHT) {
-            _raycaster->setInteractionMode(BlockInteractionMode::PlaceIntent);
-        }
-        if (me->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_LEFT) {
-            _raycaster->setInteractionMode(BlockInteractionMode::BreakHold);
-            _breakAccum = 0.0f;
-            _raycaster->setBreakProgress(0.0f);
-        }
-    };
-    ml->onMouseUp = [this](ax::Event* event) {
-        auto* me = static_cast<ax::EventMouse*>(event);
-        if (me->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_RIGHT) {
-            _raycaster->placeBlock(_selectedBlockId);
-            _raycaster->setInteractionMode(BlockInteractionMode::Look);
-        }
-        if (me->getMouseButton() == ax::EventMouse::MouseButton::BUTTON_LEFT) {
-            _raycaster->setInteractionMode(BlockInteractionMode::Look);
-            _breakAccum = 0.0f;
-            _raycaster->setBreakProgress(0.0f);
-        }
-    };
-    _eventDispatcher->addEventListenerWithSceneGraphPriority(ml, this);
-}
-
-// ============================================================================
-//  processBreakProgress — накопление прогресса разрушения
-// ============================================================================
-void GameScene::processBreakProgress(float dt)
-{
-    if (_raycaster->getInteractionMode() != BlockInteractionMode::BreakHold)
-        return;
-    if (!_raycaster->hasHit())
-        return;
-    _breakAccum += dt;
-    float progress = std::min(_breakAccum / BREAK_TIME, 1.0f);
-    _raycaster->setBreakProgress(progress);
-    if (progress >= 1.0f) {
-        _raycaster->breakBlock();
-        _breakAccum = 0.0f;
-        _raycaster->setBreakProgress(0.0f);
-    }
-}
-
 // ============================================================================
 //  update — главный игровой цикл
 // ============================================================================
@@ -181,8 +122,6 @@ void GameScene::update(float dt)
 ax::Vec3 playerPos = _playerController ? _playerController->getPlayerPosition() : ax::Vec3::ZERO;
 _chunkMgr.update(playerPos);
-// Обрабатываем прогресс разрушения
-processBreakProgress(dt);
 }
 
@@ -212,12 +151,8 @@ ax::Vec3 GameScene::getPlayerPosition() const
 
 void GameScene::onKeyPressed(ax::EventKeyboard::KeyCode keyCode, ax::Event* event)
 {
-if (keyCode == ax::EventKeyboard::KeyCode::KEY_F12) {
-    auto scene = GameScene::create();
-    ax::Director::getInstance()->replaceScene(scene);
-}
-if (keyCode == ax::EventKeyboard::KeyCode::KEY_1) _selectedBlockId = BLOCK_STONE;
-if (keyCode == ax::EventKeyboard::KeyCode::KEY_2) _selectedBlockId = BLOCK_DIRT;
-if (keyCode == ax::EventKeyboard::KeyCode::KEY_3) _selectedBlockId = BLOCK_GRASS;
+if (keyCode == ax::EventKeyboard::KeyCode::KEY_F12) {
+    ax::Director::getInstance()->replaceScene(GameScene::create());
+}
 }
 
diff --git a/VoxelRaycaster.cpp b/VoxelRaycaster.cpp
index 1234567..abcdefg 100644
--- a/VoxelRaycaster.cpp
+++ b/VoxelRaycaster.cpp
@@ -33,7 +33,8 @@ bool VoxelRaycasterNode::init(ax::Camera* camera, ChunkManager* mgr, float maxDi
 if (!_terrainAtlas) {
     // ... fallback текстура ...
 }
-scheduleUpdate();
+// Приоритет 1: выполняется ВТОРЫМ, после обновления позиции камеры в FPC (приоритет 0)
+scheduleUpdateWithPriority(1);
 return true;
 }
